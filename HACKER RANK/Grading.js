function gradingStudents(grades) {
  for (let i = 0; i <= grades.length; i++) console.log(grades[i] % 5);
  // if (grades[i] >= 38) {
  // if (grades[i] % 5 >= 2) {
  // }
}

let grades = [73, 67, 38, 33];
gradingStudents(grades);
